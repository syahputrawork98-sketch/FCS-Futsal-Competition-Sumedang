import styles from "./desktop-bracket.module.css";
// import type { HomeMatch } from "../../types/home.types";

// In a real implementation with valid data, this would take the matches
// and render them in a bracket structure using standard HTML/CSS.
export function DesktopBracket() {
  return (
    <div className={styles.container}>
      <p>Data bracket fase gugur belum tersedia secara lengkap.</p>
    </div>
  );
}
